{
  "name": "memorygame",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "sync": "vite build && npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "generate:icons": "npx capacitor-assets generate --android --ios --assetPath resources"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "vite": "^6.0.0",
    "vite-plugin-static-copy": "^4.1.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "@capacitor/ios": "^8.3.4"
  }
}
